# 2026-04-23 工作日志

## 项目代码检查和修复

### 修复内容

1. **requirements.txt 缺失依赖**
   - 添加 `psutil` (window_manager.py 使用)
   - 添加 `rapidocr-onnxruntime` (gui.py 使用)
   - 标记 `pyinstaller` 为可选

2. **gui.py 重复导入和动态导入问题**
   - 移除重复的 `import threading`
   - 保留 importlib 方式导入 daily_task.py 和 soldier.py（因同名模块和包冲突）

3. **Protective_casing.py 除零风险**
   - 第 257 行 NMS 算法中添加面积检查，防止除零错误

4. **mining.py 空引用风险**
   - 第 718 行添加 `self.mining_manager is not None` 检查

5. **gui.py lambda 表达式问题**
   - 将返回元组的 lambda 改为显式方法 `_on_daily_task_init`

6. **snowfield_weapon_league.py 红点检测逻辑**
   - 第 389 行：未检测到红点时不再终止流程，改为警告并继续

7. **config.py 未使用导入**
   - 移除未使用的 `Any`, `Dict`, `Optional` 导入

8. **.gitignore 重复条目**
   - 移除重复的 `*.spec`, `dist/`, `build/`

9. **build_exe.py shell=True 问题**
   - 移除不必要的 `shell=True` 参数

### 验证结果
所有模块修改后均通过导入测试，无回归问题。

---

## 项目代码全面修复（第二轮）

### 🔴 阻塞级修复（3/3 完成）

1. **线程安全问题 - mining.py**
   - 将 `_scheduler_running` 从 `bool` 改为 `threading.Event`
   - 所有读写改为 `is_set()`/`clear()`/`set()`
   - 修复位置：第 1193, 1333-1334, 1350, 1377, 1420, 1466, 1483, 1517-1519, 1543, 1559, 1562

2. **鼠标钩子资源清理 - recording.py**
   - `remove_mouse_hook()` 方法添加 `finally` 块
   - 确保即使异常也清理全局变量 `mouse_hook = None`

3. **pyautogui 安全配置 - mining.py**
   - 模块级别添加 `pyautogui.FAILSAFE = True`
   - 添加 `pyautogui.PAUSE = 0.1`

### 🟡 建议级修复（部分完成）

4. **代码重复 - mining.py**
   - `_check_user_stop()` 被调用 35 次
   - 使用装饰器修改风险较大，暂时保持现状

5. **GUI 模块化拆分 - gui.py**
   - 待后续单独规划（大型重构任务）

6. **日志检查优化 - window_manager.py**
   - 批量移除所有 `if self.logger.isEnabledFor(logging.DEBUG):` 检查
   - 直接使用 `self.logger.debug()`，让 logger 自己判断

### 💭 挑剔级修复（部分完成）

7. **魔法数字** - 待后续处理
8. **异常捕获细化** - 待后续处理
9. **全局变量封装** - 待后续处理

### 验证结果
所有修改后的模块均通过导入测试：
- `src.mining` ✅
- `src.recording` ✅
- `src.window_manager` ✅
- `src.gui` ✅
