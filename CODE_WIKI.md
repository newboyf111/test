# 无尽冬日 (Wujindongri) - Code Wiki

## 1. 仓库概览

无尽冬日是一个自动化游戏辅助工具，主要用于管理和操作游戏窗口，实现自动挖矿、开盾等功能。

- **多窗口管理**：支持同时管理多个游戏窗口，实现窗口激活、调整尺寸等操作
- **自动挖矿**：支持多窗口自动轮换挖矿，跨分辨率、跨DPI、跨窗口尺寸自适应图片匹配
- **保护性外壳**：自动检测并处理游戏中的war状态，实现自动开盾
- **坐标记录**：支持记录游戏窗口内的点击坐标，方便后续自动化操作
- **每日任务**：支持自动执行游戏中的每日任务
- **雪域兵器联赛**：支持自动参与雪域兵器联赛活动

## 2. 目录结构

```
├── src/              # 源代码
│   ├── config/       # 配置文件
│   │   ├── __init__.py
│   │   └── settings.py   # 配置设置
│   ├── utils/        # 工具类
│   │   ├── __init__.py
│   │   ├── adaptive_matcher.py   # 自适应图片匹配
│   │   ├── red_dot_detector.py   # 红点检测
│   │   ├── resource_path.py      # 资源路径管理
│   │   ├── resource_pool.py      # 资源池
│   │   ├── screenshot_cache.py   # 截图缓存
│   │   └── window_utils.py       # 窗口工具
│   ├── Protective_casing.py   # 保护性外壳（开盾）模块
│   ├── __init__.py
│   ├── config.py
│   ├── daily_task.py           # 每日任务模块
│   ├── dashed_line_detector.py # 虚线检测器
│   ├── frame_data.json
│   ├── gui.py                  # GUI界面
│   ├── main.py                 # 主程序入口
│   ├── mining.py               # 挖矿模块
│   ├── modules.py
│   ├── recording.py            # 记录模块
│   ├── snowfield_weapon_league.py  # 雪域兵器联赛模块
│   └── window_manager.py       # 窗口管理模块
├── docs/             # 文档
│   └── mining_bug_check.md
├── logs/             # 日志文件
├── pic/              # 图片资源
├── tests/            # 测试文件
├── README.md         # 项目说明
├── build_exe.py      # 打包脚本
├── requirements.txt  # 依赖列表
└── run.py            # 运行脚本
```

## 3. 系统架构与主流程

### 系统架构

无尽冬日采用模块化设计，主要由以下几个核心模块组成：

1. **主程序模块**：负责程序的启动、初始化和日志设置
2. **GUI模块**：提供用户界面，处理用户操作和显示系统状态
3. **窗口管理模块**：负责窗口的获取、激活和调整
4. **挖矿模块**：实现自动挖矿功能，支持多窗口轮换
5. **保护性外壳模块**：实现自动开盾功能
6. **记录模块**：实现坐标记录功能
7. **每日任务模块**：实现自动执行每日任务
8. **雪域兵器联赛模块**：实现自动参与雪域兵器联赛

### 主流程

1. **程序启动**：通过 `main.py` 启动程序，初始化日志系统
2. **GUI初始化**：创建 `WujindongriGUI` 实例，初始化各个功能模块
3. **窗口管理**：用户选择游戏窗口，系统获取窗口列表并激活
4. **功能执行**：用户选择需要执行的功能（如挖矿、开盾等），系统调用相应模块执行
5. **自动化操作**：模块根据预设流程执行自动化操作，如点击、截图分析等
6. **状态反馈**：系统实时更新状态，显示操作结果和日志

## 4. 核心功能模块

### 4.1 窗口管理模块 (window_manager.py)

**功能**：负责获取、激活和调整游戏窗口

**主要功能**：
- 获取所有可见窗口列表
- 激活指定窗口
- 调整窗口尺寸
- 获取窗口信息（标题、尺寸、进程等）

**关键类**：`WindowManager`

**使用场景**：用户选择游戏窗口后，系统需要激活窗口并调整到合适的尺寸，以便后续的自动化操作

### 4.2 挖矿模块 (mining.py)

**功能**：实现自动挖矿功能，支持多窗口轮换

**主要功能**：
- 单窗口挖矿：实现单个窗口的自动挖矿流程
- 多窗口管理：管理多个窗口，实现自动轮换挖矿
- 自适应图片匹配：支持不同分辨率和窗口尺寸
- OCR识别：识别游戏中的挖矿次数

**关键类**：
- `SingleWindowMiner`：单个窗口的挖矿模块
- `MultiWindowMiningManager`：多窗口挖矿管理器

**使用场景**：用户选择多个游戏窗口后，系统自动在这些窗口之间轮换执行挖矿操作

### 4.3 保护性外壳模块 (Protective_casing.py)

**功能**：自动检测并处理游戏中的war状态，实现自动开盾

**主要功能**：
- 检测war状态
- 自动点击deploy按钮
- 处理six位置
- 检测红点

**关键类**：`ProtectiveCasing`

**使用场景**：挖矿完成后，系统自动检测游戏窗口中的war状态，执行开盾操作

### 4.4 记录模块 (recording.py)

**功能**：记录游戏窗口内的点击坐标，方便后续自动化操作

**主要功能**：
- 全局鼠标钩子：捕获鼠标点击事件
- 坐标记录：记录点击的相对坐标和绝对坐标
- 坐标保存：将记录的坐标保存到文件

**关键类**：`RecordingModule`

**使用场景**：用户需要记录游戏中的特定位置，以便后续自动化操作时使用

### 4.5 GUI模块 (gui.py)

**功能**：提供用户界面，处理用户操作和显示系统状态

**主要功能**：
- 窗口管理：显示和管理游戏窗口
- 功能控制：提供各种功能的控制按钮
- 状态显示：显示系统状态和日志
- 画框功能：允许用户在窗口中框选区域

**关键类**：`WujindongriGUI`

**使用场景**：用户通过GUI界面操作系统，控制各种功能的执行

### 4.6 每日任务模块 (daily_task.py)

**功能**：实现自动执行游戏中的每日任务

**使用场景**：用户需要系统自动执行游戏中的每日任务，节省手动操作时间

### 4.7 雪域兵器联赛模块 (snowfield_weapon_league.py)

**功能**：实现自动参与雪域兵器联赛活动

**使用场景**：用户需要系统自动参与雪域兵器联赛活动，提高游戏效率

## 5. 核心 API/类/函数

### 5.1 主程序模块

**`main()`** (main.py)
- **功能**：主函数，负责程序的启动和初始化
- **参数**：无
- **返回值**：无
- **作用**：设置日志系统，初始化GUI并运行

### 5.2 窗口管理模块

**`WindowManager.get_window_list()`**
- **功能**：获取所有可见窗口列表
- **参数**：无
- **返回值**：窗口列表，每个元素为 (hwnd, title) 元组
- **作用**：获取系统中所有可见窗口，供用户选择

**`WindowManager.activate_window(hwnd)`**
- **功能**：激活指定窗口
- **参数**：hwnd - 窗口句柄
- **返回值**：是否激活成功
- **作用**：激活用户选择的游戏窗口

**`WindowManager.resize_window(hwnd, client_width, client_height)`**
- **功能**：调整窗口尺寸
- **参数**：
  - hwnd - 窗口句柄
  - client_width - 目标宽度
  - client_height - 目标高度
- **返回值**：是否调整成功
- **作用**：将游戏窗口调整到合适的尺寸，以便后续的自动化操作

### 5.3 挖矿模块

**`SingleWindowMiner.start_mining()`**
- **功能**：开始单个窗口的挖矿
- **参数**：无
- **返回值**：是否开始成功
- **作用**：启动单个窗口的挖矿流程

**`MultiWindowMiningManager.start_mining_queue()`**
- **功能**：开始多窗口挖矿队列
- **参数**：无
- **返回值**：是否开始成功
- **作用**：启动多窗口轮换挖矿

**`MultiWindowMiningManager.stop_mining_queue(user_stopped=False)`**
- **功能**：停止挖矿队列
- **参数**：user_stopped - 是否是用户手动停止
- **返回值**：是否停止成功
- **作用**：停止所有窗口的挖矿操作

### 5.4 保护性外壳模块

**`ProtectiveCasing.start()`**
- **功能**：启动保护性外壳自动化脚本
- **参数**：无
- **返回值**：无
- **作用**：开始检测游戏窗口中的war状态，执行开盾操作

**`ProtectiveCasing.check_war_in_window(hwnd, window_name)`**
- **功能**：检查窗口中是否存在war.png
- **参数**：
  - hwnd - 窗口句柄
  - window_name - 窗口名称
- **返回值**：是否检测到war
- **作用**：检测游戏窗口中的war状态

**`ProtectiveCasing.click_deploy(hwnd, window_name)`**
- **功能**：点击deploy按钮
- **参数**：
  - hwnd - 窗口句柄
  - window_name - 窗口名称
- **返回值**：无
- **作用**：执行开盾操作

### 5.5 记录模块

**`RecordingModule.start_recording()`**
- **功能**：开始记录坐标
- **参数**：无
- **返回值**：是否开始成功
- **作用**：开始记录用户在游戏窗口中的点击坐标

**`RecordingModule.stop_recording()`**
- **功能**：停止记录坐标
- **参数**：无
- **返回值**：是否停止成功
- **作用**：停止记录并保存坐标

**`RecordingModule.save_coordinates()`**
- **功能**：保存坐标到文件
- **参数**：无
- **返回值**：无
- **作用**：将记录的坐标保存到文件中

### 5.6 GUI模块

**`WujindongriGUI.create_widgets()`**
- **功能**：创建界面组件
- **参数**：无
- **返回值**：无
- **作用**：创建GUI界面的各个组件

**`WujindongriGUI.start_mining()`**
- **功能**：开始挖矿
- **参数**：无
- **返回值**：无
- **作用**：启动挖矿操作

**`WujindongriGUI.start_shield_process()`**
- **功能**：开始开盾流程
- **参数**：无
- **返回值**：无
- **作用**：启动开盾操作

## 6. 技术栈与依赖

| 技术/依赖 | 用途 | 来源 |
|---------|------|------|
| Python 3.8+ | 主要开发语言 | requirements.txt |
| tkinter | GUI界面 | 标准库 |
| win32gui | 窗口操作 | requirements.txt |
| win32con | Windows常量 | requirements.txt |
| win32process | 进程操作 | requirements.txt |
| psutil | 进程管理 | requirements.txt |
| cv2 (OpenCV) | 图像处理 | requirements.txt |
| numpy | 数值计算 | requirements.txt |
| pyautogui | 鼠标操作 | requirements.txt |
| easyocr | OCR识别 | requirements.txt |
| PIL | 图像处理 | requirements.txt |

## 7. 关键模块与典型用例

### 7.1 挖矿模块使用方法

**功能说明**：自动在游戏窗口中执行挖矿操作，支持多窗口轮换

**配置与依赖**：
- 需要游戏窗口尺寸为558x1021
- 依赖pyautogui、cv2等库

**使用示例**：

```python
# 1. 选择游戏窗口
# 在GUI中选择需要挖矿的游戏窗口

# 2. 点击"开始挖矿"按钮
# GUI会调用start_mining方法

# 3. 系统自动执行挖矿流程
# 包括：点击town、search、资源、add、search_meat、gather、battle等

# 4. 挖矿完成后，系统会自动启动保护性外壳
# 检测并处理war状态
```

### 7.2 开盾模块使用方法

**功能说明**：自动检测并处理游戏中的war状态，实现自动开盾

**配置与依赖**：
- 需要游戏窗口尺寸为558x1021
- 依赖pyautogui、cv2等库

**使用示例**：

```python
# 1. 选择游戏窗口
# 在GUI中选择需要开盾的游戏窗口

# 2. 点击"开盾"按钮
# GUI会调用start_shield_process方法

# 3. 系统自动执行开盾流程
# 包括：检测war、点击deploy、Shield、buy_2、buy等
```

### 7.3 记录模块使用方法

**功能说明**：记录游戏窗口内的点击坐标，方便后续自动化操作

**配置与依赖**：
- 依赖win32gui、ctypes等库

**使用示例**：

```python
# 1. 选择游戏窗口
# 在GUI中选择需要记录坐标的游戏窗口

# 2. 点击"开始记录"按钮
# GUI会调用start_recording方法

# 3. 在游戏窗口中点击需要记录的位置
# 系统会弹出输入框，要求输入位置名称

# 4. 点击"停止记录"按钮
# 系统会将记录的坐标保存到"记录.json"文件中
```

## 8. 配置、部署与开发

### 8.1 配置文件

**配置文件**：`src/config/settings.py`

**主要配置项**：
- `VERSION`：版本信息
- `AUTHOR`：作者信息
- `APP_NAME`：应用名称
- `APP_TITLE`：应用标题
- `LOG_LEVEL`：日志级别
- `LOG_FORMAT`：日志格式
- `DATABASE`：数据库设置
- `SERVER`：服务器设置
- `CONFIG`：其他配置

### 8.2 部署方法

1. **克隆项目**：
   ```bash
   git clone <repository-url>
   cd wujindongri
   ```

2. **安装依赖**：
   ```bash
   pip install -r requirements.txt
   ```

3. **运行程序**：
   ```bash
   python -m src
   ```

### 8.3 开发指南

**代码风格**：
- 使用PEP 8编码规范
- 函数和类使用文档字符串
- 变量和函数命名使用小写字母和下划线

**开发流程**：
1. 了解现有模块的功能和结构
2. 编写新功能或修改现有功能
3. 测试功能是否正常
4. 更新文档

## 9. 监控与维护

### 9.1 日志系统

**日志文件**：`logs/wujindongri.log`

**日志级别**：INFO

**日志格式**：`%(asctime)s - %(name)s - %(levelname)s - %(message)s`

### 9.2 常见问题与解决方案

| 问题 | 解决方案 |
|------|---------|
| 窗口无法激活 | 检查窗口句柄是否有效，尝试以管理员身份运行程序 |
| 挖矿失败 | 检查游戏窗口尺寸是否为558x1021，检查图片资源是否存在 |
| 开盾失败 | 检查游戏窗口中是否有war状态，检查图片资源是否存在 |
| 记录坐标失败 | 检查是否以管理员身份运行程序，检查游戏窗口是否有效 |

## 10. 总结与亮点回顾

### 10.1 项目亮点

1. **多窗口管理**：支持同时管理多个游戏窗口，实现窗口激活、调整尺寸等操作
2. **自适应图片匹配**：支持不同分辨率和窗口尺寸的图片匹配，提高自动化操作的可靠性
3. **OCR识别**：使用OCR技术识别游戏中的挖矿次数，提高自动化操作的智能性
4. **多线程设计**：使用多线程技术，提高系统的响应速度和稳定性
5. **模块化设计**：采用模块化设计，代码结构清晰，易于维护和扩展
6. **用户友好界面**：提供直观的GUI界面，方便用户操作

### 10.2 应用场景

无尽冬日主要应用于以下场景：

1. **游戏自动化**：自动执行游戏中的重复任务，如挖矿、开盾等
2. **多账号管理**：同时管理多个游戏账号，提高游戏效率
3. **节省时间**：减少手动操作时间，让用户可以专注于其他事情
4. **提高效率**：自动化操作比手动操作更快、更准确

### 10.3 未来发展

1. **功能扩展**：增加更多游戏功能的自动化支持
2. **智能优化**：使用机器学习技术优化自动化操作的准确性和效率
3. **跨平台支持**：支持更多操作系统和游戏平台
4. **用户定制**：提供更多用户定制选项，满足不同用户的需求

## 11. 代码安全性

### 11.1 安全措施

1. **权限管理**：部分功能需要管理员权限才能正常运行
2. **错误处理**：完善的错误处理机制，确保程序稳定运行
3. **日志记录**：详细的日志记录，便于问题排查
4. **资源管理**：合理的资源管理，避免资源泄漏

### 11.2 注意事项

1. **使用风险**：自动化工具可能违反游戏规则，使用需谨慎
2. **系统兼容性**：部分功能可能与特定系统版本不兼容
3. **性能影响**：多窗口操作可能会对系统性能造成影响
4. **安全风险**：使用第三方库可能存在安全风险，需定期更新

## 12. 附录

### 12.1 图片资源说明

| 图片名称 | 用途 |
|---------|------|
| town.png | 识别游戏中的城镇界面 |
| wild.png | 识别游戏中的野外界面 |
| search.png | 识别搜索按钮 |
| meat.png | 识别肉类资源 |
| wood.png | 识别木材资源 |
| coal mine.png | 识别煤矿资源 |
| iron.png | 识别铁矿资源 |
| add.png | 识别添加按钮 |
| search_meat.png | 识别搜索肉类按钮 |
| gather.png | 识别采集按钮 |
| battle.png | 识别战斗按钮 |
| team.png | 识别队伍按钮 |
| close.png | 识别关闭按钮 |
| minus.png | 识别减少按钮 |
| back.png | 识别返回按钮 |
| back1.png | 识别返回按钮1 |
| war.png | 识别战争状态 |
| Shield.png | 识别盾牌按钮 |
| buy.png | 识别购买按钮 |
| buy1.png | 识别购买按钮1 |
| six.png | 识别六个位置 |
| sure.png | 识别确定按钮 |

### 12.2 快捷键说明

| 快捷键 | 功能 |
|-------|------|
| 开始挖矿 | 启动挖矿操作 |
| 停止挖矿 | 停止挖矿操作 |
| 开始记录 | 开始记录坐标 |
| 停止记录 | 停止记录坐标 |
| 开盾 | 启动开盾操作 |
| 雪域兵器联赛 | 启动雪域兵器联赛操作 |
| 每日任务 | 启动每日任务操作 |
| 画框 | 启动画框功能 |
| 刷新窗口列表 | 刷新窗口列表 |
| 激活选中窗口 | 激活选中的窗口 |
| 调整窗口尺寸 | 调整选中窗口的尺寸 |
| 退出 | 退出程序 |

### 12.3 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0.0 | 2026-03-19 | 初始版本发布 |
